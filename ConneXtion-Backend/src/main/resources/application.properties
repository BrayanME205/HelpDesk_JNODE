spring.application.name=demo
# Server
server.port=8081

# SQL Server
spring.datasource.url=jdbc:sqlserver://163.178.173.130:1433;databaseName=ConneXtionJNODE;encrypt=false;trustServerCertificate=true
spring.datasource.username=basesdedatos
spring.datasource.password=BasesRP2025
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Desactivar Spring Security
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Sesiones HTTP
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax